// TODO: Implement Projects tab sections:
//   - Grid of ProjectEntry cards with title, description, tech tags, links
//   - Filter/sort by tech stack or date
import type { ProjectEntry } from "@/types";

export default function ProjectsSections() {
  return null;
}

export function ProjectCard(_props: { project: ProjectEntry }) {
  return null;
}
